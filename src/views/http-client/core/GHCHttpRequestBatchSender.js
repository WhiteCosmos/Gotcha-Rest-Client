/**
 * @created_at 2022/10/19
 * @app_version 1.5.4
 */
class GHCHttpRequestBatchSender {
    constructor(httpRequest,
                cookieJar,
                dynamicVariableRender) {

    }

    sendHttpRequests() {
        // 1. 查询所有http request

        // 2. 构建请求并发送，设置超时时间30秒，如果请求已打开，发送相关事件

        // 3. 收到响应并更新http operation，保存
    }
}

export default GHCHttpRequestBatchSender