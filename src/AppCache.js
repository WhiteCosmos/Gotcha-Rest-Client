/**
 * @created_at 2022/10/11
 * @app_version 1.5.3
 */
let AppCache = {
    httpOperations: new Map(),

    // 测试使用

    jsonSchemaGroups: [],

    jsonSchemaPool: new Map(),
}

export default AppCache